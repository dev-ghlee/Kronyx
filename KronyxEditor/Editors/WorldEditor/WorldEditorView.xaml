<UserControl x:Class="KronyxEditor.Editors.WorldEditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:docking="clr-namespace:KronyxEditor.Docking"
             mc:Ignorable="d" Focusable="True"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.InputBindings>
        <KeyBinding Key="Z" Modifiers="Ctrl" Command="{Binding UndoCommand}"/>
        <KeyBinding Key="Y" Modifiers="Ctrl" Command="{Binding RedoCommand}"/>
        <KeyBinding Key="S" Modifiers="Ctrl" Command="{Binding SaveCommand}"/>
    </UserControl.InputBindings>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="32"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal">
            <Button Content="Undo" Command="{Binding UndoCommand}"/>
            <Button Content="Redo" Command="{Binding RedoCommand}" Margin="8,0,0,0"/>
            <Button Content="Save" Command="{Binding SaveCommand}" Margin="8,0,0,0"/>
            <Button Content="Save Layout" Margin="16,0,0,0" Click="OnSaveLayoutClick"/>
            <Button Content="Load Layout" Margin="8,0,0,0" Click="OnLoadLayoutClick"/>
        </StackPanel>

        <Grid Grid.Row="1" x:Name="DockWorkspaceHost"/>
    </Grid>
</UserControl>
